"use client";
import withAuth from "@/app/_utils/withAuth";

const Dashboard = () => {
    return (
        <div>
            <h1>Dashboard</h1>
        </div>
    )
}

export default withAuth(Dashboard);